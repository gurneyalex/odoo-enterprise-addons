===========================
Sale Timesheet Rounded Grid
===========================

This module closes the gap between OCA module `sale_timesheet_rounded` and
Odoo Enterprise module `timesheet_grid` to ensure the field `unit_amount_rounded`
on `account.analytic.line` is correctly updated.
